env:
  name: "Acrobot-v1"
  seed: 42

agent:
  type: "dqn"
  network_hidden: [64]
  alpha: 0.001
  gamma: 0.99
  epsilon_decay: 5
  epsilon_min: 0.01
  batch_size: 64
  buffer_size: 10000
  target_update_tau: 0.01

trainer:
  episodes: 500

evaluator:
  episodes: 100 

reporting:  
  log_every: 50 